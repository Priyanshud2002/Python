def add_numbers(a, b): #creates a function add_numbers
    return a + b #returns output by adding a + b
result = add_numbers(3, 5) # result stores the return of function add numbers
print(result) #prints the result
